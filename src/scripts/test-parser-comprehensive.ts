import { MetadataParser } from '../lib/telegram/parser';
import dotenv from 'dotenv';

// Загружаем переменные окружения
dotenv.config();

// Тестовые примеры
const testCases = [
  {
    name: "Цикл Пособие для Тёмного князя",
    text: `Автор: Ольга Болдырева

Название: цикл Пособие для Тёмного князя

Жанр: 
#фэнтези
#героическоефэнтези
#приключенческое
#закончен

Рейтинг: 7.05 #выше7

Если Темный Властелин страдает скукой — плохо всем вокруг. Однако возьмем одну команду героев, идущих по его голову — и вот уже тебе и приключения, и враги, и даже неожиданные открытия. Настолько, что приходится перестать играться с «новыми игрушками», и серьезно взяться за беспорядки, творящиеся в собственных землях. Тут главное не перестараться. Ведь цена ошибки — жизни дорогих тебе существ.

Состав:
Как развеять скуку (2010)
Как раздать долги (2013)`
  },
  {
    name: "Классический формат",
    text: `Автор: Сергей Ткачев
Название: цикл Дримеры
Жанр: #фэнтези, #боевик, #ЛитРПГ, #приключенческое, #закончен
Рейтинг: 4.5
Черная полоса. У каждого она бывает. Но иногда этот жизненный период затягивается настолько, что конца и края ему не видно.

Состав:
1. По ту сторону реальности (2019)
2. Наследие падших (2019)
3. Сон падших (2019)
4. Дрожь времени (2019)
5. Власть (2020)`
  }
];

console.log('🔍 Комплексное тестирование парсера метаданных\n');

for (const testCase of testCases) {
  console.log(`--- Тест: ${testCase.name} ---`);
  const metadata = MetadataParser.parseMessage(testCase.text);
  
  console.log(`Автор: "${metadata.author}"`);
  console.log(`Название: "${metadata.title}"`);
  console.log(`Рейтинг: ${metadata.rating}`);
  console.log(`Жанры: [${metadata.genres.map(g => `"${g}"`).join(', ')}]`);
  console.log(`Теги: [${metadata.tags.map(t => `"${t}"`).join(', ')}]`);
  console.log(`Описание: "${metadata.description.substring(0, 100)}${metadata.description.length > 100 ? '...' : ''}"`);
  console.log(`Книги в серии: ${metadata.books.length}`);
  
  for (const book of metadata.books) {
    console.log(`  - ${book.title} (${book.year})`);
  }
  
  console.log('');
}