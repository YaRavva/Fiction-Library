# План тестирования Telegram Client API для Fiction Library

## 🎯 Целевые каналы:
1. **Канал метаданных:** (публичный канал с описаниями книг)
2. **Канал файлов:** (приватный канал с ZIP архивами)

## 🔑 API Credentials:
- **Конфигурация:** Данные хранятся в `.env` файле
- **Метод:** Ручной подход с TypeScript/Node.js

## 🔧 Выбранный метод: Telegram Client API (MTProto)

### Преимущества для нашего случая:
- ✅ Читает историю сообщений (не только новые)
- ✅ Работает с публичными каналами без админских прав
- ✅ Поддерживает файлы размером до 2ГБ
- ✅ Может работать с приватными каналами (если есть доступ)
- ✅ Стабильный официальный протокол

### Библиотеки для интеграции:
1. **GramJS (Node.js)** - основная для Next.js интеграции
2. **node-telegram-bot-api** - альтернатива
3. **TypeScript** - типизация для безопасности

## 📋 Ручной план действий:

### Фаза 1: Анализ и планирование ✅
1. ✅ **API credentials получены**
2. ✅ **Метод выбран:** Telegram Client API (ручной)
3. ✅ **Изучить структуру данных** в каналах вручную
4. ✅ **Создать схему парсинга** метаданных (завершено в `src/lib/telegram/parser.ts`)

### Фаза 2: Имплементация
5. ✅ **Создать TypeScript/Node.js скрипт** для работы с Telegram API (готово — `src/scripts/telegram-login.ts`)
6. ✅ **Реализовать парсер метаданных** (готово — `src/lib/telegram/parser.ts`)
7. ✅ **Настроить скачивание файлов** (завершено в `src/lib/telegram/sync.ts`, `downloadMedia` использует GramJS)
8. ✅ **Интегрировать с Next.js приложением** (готово — базовая интеграция)

### Фаза 3: База данных и UI
9. ✅ **Настроить Supabase схему** для хранения метаданных и связей (готово — миграции созданы)
10. ✅ **Создать UI компоненты** для просмотра библиотеки (готово — `src/app/library/page.tsx`)
11. ✅ **Добавить поиск и фильтрацию** по метаданным (готово — поиск реализован)
12. 🔄 **Реализовать систему очередей** для скачивания файлов (в процессе — таблицы созданы, нужен UI)

### Фаза 4: Синхронизация и автоматизация
13. ✅ **Создать API endpoint** для запуска синхронизации с Telegram (`/api/admin/sync`)
14. ✅ **Реализовать UI для управления синхронизацией** (админ панель `/admin`)
15. ✅ **Добавить мониторинг очереди загрузок** (готово — компонент и API)
16. ⏳ **Настроить автоматическую синхронизацию** (cron job или webhook)

### Фаза 5: Тестирование и оптимизация
17. 🔄 **Протестировать синхронизацию** с реальным Telegram каналом
18. ⏳ **Реализовать загрузку файлов** из приватного канала
19. ⏳ **Добавить обработку ошибок** и retry логику
20. ⏳ **Оптимизировать производительность** загрузки файлов

Готов начать разработку на основе этого плана!

## ⚠️ Ограничения и особенности:

### Приватный канал:
- Требует активного участия в канале
- Нужно принять инвайт по ссылке
- Может потребовать подтверждение администратора

### Rate Limits:
- ~30 запросов в секунду
- Ограничения на загрузку файлов
- Flood wait в случае превышения

### Безопасность:
- Номер телефона будет виден администраторам каналов
- Сессия сохраняется локально
- Двухфакторная аутентификация (если включена)

## 🧪 TypeScript сценарии:

### Сценарий 1: Публичный канал (fantastieka1)
```typescript
// Подключение к публичному каналу
const channel = await client.getEntity('fantastieka1');
const messages = await client.getMessages(channel, { limit: 10 });
```

### Сценарий 2: Приватный канал (инвайт)
```typescript
// Присоединение к приватному каналу
const hashPart = 's-DnVIsIcjc0NjYy'; // из ссылки
await client.invoke(new Api.messages.ImportChatInvite({ hash: hashPart }));
```

### Сценарий 3: Парсинг метаданных
```typescript
// Извлечение данных книги из сообщения
const text = message.text;
const author = extractAuthor(text);
const title = extractTitle(text);
const rating = extractRating(text);
const tags = extractTags(text);
```

### Сценарий 4: Загрузка файлов
```typescript
// Скачивание ZIP файла
if (message.document) {
  const buffer = await client.downloadMedia(message);
  await extractFb2Files(buffer);
}
```

## 📝 Текущий прогресс

✅ **Базовая инфраструктура:**
- ✅ Next.js проект создан и настроен
- ✅ API credentials получены и протестированы
- ✅ Базовая авторизация через GitHub настроена
- ✅ Supabase подключен и инициализирован

✅ **Telegram интеграция:**
- ✅ GramJS интеграция работает
- ✅ Парсер метаданных реализован
- ✅ Скачивание файлов работает
- ✅ Тесты и временные файлы очищены

✅ **База данных:**
- ✅ Схема Supabase создана (books, series, user_profiles, и т.д.)
- ✅ Миграции подготовлены (001-005)
- ✅ RLS политики настроены
- ✅ Индексы для поиска созданы

✅ **UI компоненты:**
- ✅ Страница библиотеки с карточками книг
- ✅ Поиск по названию, автору, описанию
- ✅ Пагинация (20 книг на страницу)
- ✅ Счетчик загрузок

✅ **Синхронизация и очереди:**
- ✅ API endpoint `/api/admin/sync` (POST для запуска, GET для статуса)
- ✅ API endpoint `/api/admin/download-queue` (управление очередью загрузок)
- ✅ Админ панель `/admin` с UI для управления
- ✅ Компонент мониторинга очереди загрузок
- ✅ Защита админских роутов (проверка роли admin)
- ✅ Статистика по книгам и сериям
- ✅ История синхронизации

⏳ **Следующие шаги (Фаза 5 - Тестирование):**
1. 🔄 **Протестировать синхронизацию** с реальным Telegram каналом метаданных
2. 🔄 **Проверить парсинг** метаданных из сообщений
3. ⏳ **Реализовать загрузку файлов** из приватного канала
4. ⏳ **Добавить worker** для обработки очереди загрузок
5. ⏳ **Настроить автоматическую синхронизацию** (cron job)
6. ⏳ **Добавить логирование** и улучшенную обработку ошибок

**Текущее состояние:**
- ✅ Вся инфраструктура готова (база данных, API, UI)
- ✅ Админ панель с мониторингом работает
- 🔄 Следующий этап — тестирование с реальными данными из Telegram
- ⏳ После тестирования — добавление worker'а для автоматической обработки очереди

**Как протестировать:**

**Шаг 1: Проверить подключение к Telegram**
```powershell
npx tsx src/scripts/test-telegram-connection.ts
```

**Шаг 2: Установить роль админа в Supabase**
```sql
UPDATE user_profiles SET role = 'admin' WHERE id = 'ваш-user-id';
```

**Шаг 3: Запустить синхронизацию через админ панель**
1. Открыть админ панель: http://localhost:3001/admin
2. Указать количество сообщений (начать с 5)
3. Нажать "Запустить синхронизацию"
4. Проверить результаты в библиотеке: http://localhost:3001/library