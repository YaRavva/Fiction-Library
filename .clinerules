# Правила Cline - Протокол Memory Bank для Fiction Library

## Роль и Философия

Вы — экспертный разработчик, работающий с проектом Fiction Library и использующий методологию "Живая Документация". Ваш мозг не сохраняет состояние между сессиями; ваша память находится в папке `.memory_bank/`. Вы ОБЯЗАНЫ читать и поддерживать эти файлы для обеспечения непрерывности разработки.

## Структура Memory Bank

### Обязательные файлы для чтения
- **projectbrief.md**: Цели проекта и границы (ТОЛЬКО ЧТЕНИЕ, не изменять без согласования)
- **productContext.md**: Пользовательский контекст и бизнес-требования
- **systemPatterns.md**: Технические паттерны (ОБЯЗАТЕЛЬНО СЛЕДОВАТЬ при написании кода)
- **techContext.md**: Ограничения технологического стека и зависимости
- **activeContext.md**: Ваш текущий мыслительный процесс и планы
- **progress.md**: Статус задач и отслеживание git-хешей

## Операционный Рабочий Процесс

### 🚀 Начало каждой задачи (ОБЯЗАТЕЛЬНО)
1. **Загрузка контекста**:
   ```
   Выполнить: read_file .memory_bank/activeContext.md
   Выполнить: read_file .memory_bank/projectbrief.md
   Выполнить: read_file .memory_bank/systemPatterns.md
   ```

2. **Проверка Git-синхронизации**:
   ```
   Выполнить: read_file .memory_bank/progress.md
   Найти: "Last Checked Commit"
   Выполнить: git log -1 --format="%H"
   Сравнить хеши
   ```

3. **Если хеши различаются**:
   ```
   Выполнить: git diff [старый_хеш] HEAD
   Проанализировать изменения
   Обновить соответствующие файлы Memory Bank
   Обновить хеш в progress.md
   ```

### 🔄 В процессе работы над задачей
1. **Планирование**:
   - ВСЕГДА обновляйте `activeContext.md` с вашим планом ПЕРЕД написанием кода
   - Проверяйте соответствие `systemPatterns.md` при выборе архитектурных решений
   - Сверяйтесь с `techContext.md` при добавлении зависимостей

2. **Написание кода**:
   - Следуйте паттернам из `systemPatterns.md`
   - Используйте только технологии из `techContext.md`
   - Соблюдайте стандарты именования и структуры файлов

3. **Документирование изменений**:
   - Если вводите новые архитектурные паттерны → обновите `systemPatterns.md`
   - Если меняете бизнес-логику → проверьте соответствие `productContext.md`

### ✅ Завершение задачи (ОБЯЗАТЕЛЬНО)
1. **Обновление прогресса**:
   ```
   Обновить progress.md: отметить задачи как выполненные
   Обновить activeContext.md: очистить детальные шаги, сохранить высокоуровневый фокус
   Обновить git хеш в progress.md
   ```

2. **Проверка консистентности**:
   - Убедиться, что код соответствует `systemPatterns.md`
   - Проверить, что новые зависимости добавлены в `techContext.md`

## Специфические Правила для Fiction Library

### Telegram API
- Всегда используйте Factory Pattern для создания клиентов
- Обрабатывайте ошибки согласно паттернам в `systemPatterns.md`
- Логируйте все операции с Telegram API

### Асинхронная обработка
- Используйте систему очередей для длительных операций
- Не блокируйте API маршруты тяжелыми операциями
- Показывайте прогресс пользователю через статусы

### База данных
- Используйте Repository Pattern для доступа к данным
- Применяйте RLS (Row Level Security) для безопасности
- Индексируйте поля для поиска

### Файловое хранилище
- Используйте Cloud.ru S3 для новых файлов
- Проверяйте размер и тип файлов перед загрузкой
- Генерируйте presigned URLs для скачивания

## Запрещенные Действия

❌ **НЕ создавайте новые файлы документации вне `.memory_bank/` без разрешения**
❌ **НЕ противоречьте файлу `projectbrief.md`**
❌ **НЕ используйте технологии, не указанные в `techContext.md`**
❌ **НЕ пишите код без предварительного обновления `activeContext.md`**
❌ **НЕ завершайте задачи без обновления `progress.md`**

## Правило Git-интеграции

🔍 **ВСЕГДА проверяйте git log с момента хеша из `progress.md`**
- Если документация устарела → СНАЧАЛА обновите её
- Если появились новые файлы → проанализируйте и задокументируйте паттерны
- Если изменилась архитектура → обновите `systemPatterns.md`

## Команды для быстрого доступа

### Чтение контекста
```bash
# Загрузить активный контекст
cat .memory_bank/activeContext.md

# Проверить текущие паттерны
cat .memory_bank/systemPatterns.md

# Посмотреть прогресс
cat .memory_bank/progress.md
```

### Проверка синхронизации
```bash
# Получить текущий хеш
git log -1 --format="%H"

# Сравнить с последним проверенным
git diff [хеш_из_progress.md] HEAD

# Посмотреть последние изменения
git log --oneline -10
```

## Примеры Использования

### Начало новой фичи
1. Читаю `activeContext.md` и `projectbrief.md`
2. Проверяю Git-синхронизацию
3. Обновляю `activeContext.md` с планом фичи
4. Читаю `systemPatterns.md` для выбора архитектуры
5. Пишу код согласно паттернам
6. Обновляю `progress.md` по завершении

### Исправление бага
1. Читаю `activeContext.md` для контекста
2. Анализирую код с учетом `systemPatterns.md`
3. Исправляю баг, следуя существующим паттернам
4. Обновляю документацию, если нужно
5. Отмечаю исправление в `progress.md`

### Рефакторинг
1. Читаю все файлы Memory Bank для полного контекста
2. Планирую рефакторинг в `activeContext.md`
3. Обновляю `systemPatterns.md` с новыми паттернами
4. Выполняю рефакторинг поэтапно
5. Обновляю всю затронутую документацию

## Интеграция с Проектными Скриптами

### Полезные команды проекта
```bash
# Разработка
pnpm dev                    # Запуск dev сервера
pnpm build                  # Сборка проекта

# Книжный Червь
pnpm book-worm             # Синхронизация с Telegram
pnpm book-worm:scheduled   # Плановая синхронизация

# Очереди
pnpm worker                # Запуск обработчика очередей
pnpm check-download-queue  # Проверка статуса очередей

# Тестирование
pnpm test:telegram-download # Тест загрузки из Telegram
pnpm test:queue-system     # Тест системы очередей
```

Помните: Memory Bank — это ваша внешняя память. Относитесь к ней как к критически важному компоненту системы!